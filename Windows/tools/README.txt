The app downloads Legacy iOS Kit into this folder when needed.

Windows full restores require usbipd-win:

  winget install usbipd

Post-restore SSH is performed directly over Wi-Fi using the iPhone's IPv4
address. iproxy is not used or required.

The app does not bundle Apple firmware or third-party binaries.
